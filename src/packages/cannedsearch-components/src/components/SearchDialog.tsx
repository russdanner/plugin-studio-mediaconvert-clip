import React, { useEffect, useState } from 'react';
import SearchUI from '@craftercms/studio-ui/components/Search/SearchUI';

export function SearchDialog(props) {
  const searchUIProps = {} as any;

  return <SearchUI {...searchUIProps} />;
}

export default SearchDialog;
